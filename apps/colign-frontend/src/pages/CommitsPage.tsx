/**
 * Commits — blank canvas. Intentionally empty for now; content gets built up
 * here over time. The standard page container is kept so future content inherits
 * the same padding + max width as the rest of the app.
 */
export function CommitsPage() {
  return <div className="p-6 sm:p-8 max-w-4xl mx-auto space-y-6" />;
}
